# ── Core Airflow pin ──────────────────────────────────────────────────────────
# Must match the base image version so pip never upgrades/downgrades core airflow
apache-airflow==2.10.4

# ── Airflow providers (versions from constraints-2.10.4/constraints-3.11.txt) ─
apache-airflow-providers-apache-spark==4.11.3
apache-airflow-providers-celery==3.8.5
apache-airflow-providers-redis==3.8.0
apache-airflow-providers-amazon==9.1.0
apache-airflow-providers-http==4.13.3

# ── gRPC / protobuf pins (from constraints-2.10.4, stops pip backtracking) ───
grpcio==1.68.1
grpcio-status==1.62.3
googleapis-common-protos==1.66.0

# ── Data stack ────────────────────────────────────────────────────────────────
pyspark==3.5.3
delta-spark==3.1.0
dbt-duckdb==1.7.4
duckdb==1.1.3
great-expectations==0.18.19

# ── Kafka ─────────────────────────────────────────────────────────────────────
kafka-python==2.0.2

# ── AWS / MinIO ───────────────────────────────────────────────────────────────
boto3==1.35.36

# ── Utilities ─────────────────────────────────────────────────────────────────
numpy==1.26.4
python-dotenv==1.0.1
